{
  "name": "directdrivex",
  "version": "1.0.0",
  "description": "A comprehensive cloud storage management platform with integrated frontend and backend systems",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
